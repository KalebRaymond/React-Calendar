* Navigation
	> Could just do an API call that
		1. Gets all the dates that would be visible given a month and year
		2. Determines which dates that get returned have events
		3. Replaces all the info on screen instead of doing an animation

* Google Calendar
	> 